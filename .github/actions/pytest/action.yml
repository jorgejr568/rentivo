name: Pytest
description: Run the full pytest suite in parallel.

runs:
  using: composite
  steps:
    - uses: ./.github/actions/setup

    - name: Run pytest
      shell: bash
      run: uv run --no-sync python -m pytest -n auto -q
