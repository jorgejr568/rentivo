name: Ruff Format
description: Run `ruff format --check` on the repository.

runs:
  using: composite
  steps:
    - uses: ./.github/actions/setup

    - name: Run ruff format --check
      shell: bash
      run: .venv/bin/ruff format --check .
