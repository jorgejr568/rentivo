name: Complete Release Gate

on:
  pull_request:
    branches: [main]
  workflow_call:
  workflow_dispatch:

permissions:
  contents: read
  packages: read

concurrency:
  group: release-gate-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  backend:
    name: backend lint, tests, and coverage
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
      - name: Sync dependencies
        run: uv sync --all-extras --frozen
      - name: Ruff check
        run: uv run --project backend --no-sync ruff check .
      - name: Ruff format check
        run: uv run --project backend --no-sync ruff format --check .
      - name: Run backend suite with coverage
        run: |
          mkdir -p artifacts
          uv run --project backend --no-sync pytest -c backend/pyproject.toml -n auto -q \
            --cov --cov-config=backend/pyproject.toml \
            --cov-report=term-missing \
            --cov-report=xml:artifacts/backend-coverage.xml
      - name: Upload backend coverage artifact
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: backend-coverage
          path: artifacts/backend-coverage.xml
          if-no-files-found: warn
          retention-days: 14
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v7
        with:
          files: artifacts/backend-coverage.xml
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

  frontend:
    name: frontend OpenAPI, tests, typecheck, lint, and build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v7
      - name: Install uv for API contract generation
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
      - name: Sync backend dependencies
        working-directory: .
        run: uv sync --all-extras --frozen
      - name: Install Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Install frontend dependencies
        run: npm ci
      - name: Verify generated API contract is current
        run: npm run api:check
      - name: Run unit tests with coverage
        run: npm run test -- --run --coverage
      - name: Typecheck
        run: npm run typecheck
      - name: Lint
        run: npm run lint
      - name: Build
        run: npm run build
      - name: Upload frontend coverage and build artifacts
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: frontend-results
          path: |
            frontend/coverage
            frontend/dist
          if-no-files-found: warn
          retention-days: 14

  e2e:
    name: mocked frontend E2E, accessibility, and visual parity
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v7
      - name: Install Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Install frontend dependencies
        run: npm ci
      - name: Install Chromium
        run: npx playwright install --with-deps chromium
      - name: Run Playwright suite
        run: npm run e2e
      - name: Upload Playwright results
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: playwright-results
          path: frontend/test-results
          if-no-files-found: ignore
          retention-days: 14

  migrations:
    name: migration single head and clean upgrade
    runs-on: ubuntu-latest
    services:
      mariadb:
        image: mariadb:11
        env:
          MARIADB_DATABASE: rentivo_ci
          MARIADB_ROOT_PASSWORD: ci-only-root-password
        ports:
          - 3306:3306
        options: >-
          --health-cmd="healthcheck.sh --connect --innodb_initialized"
          --health-interval=5s
          --health-timeout=5s
          --health-retries=20
    env:
      RENTIVO_DB_URL: mysql+pymysql://root:ci-only-root-password@127.0.0.1:3306/rentivo_ci
      RENTIVO_ENVIRONMENT: dev
    steps:
      - uses: actions/checkout@v7
      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
      - name: Sync dependencies
        run: uv sync --all-extras --frozen
      - name: Require exactly one Alembic head
        run: |
          heads="$(uv run --project backend --no-sync alembic -c backend/alembic.ini heads)"
          printf '%s\n' "$heads"
          count="$(printf '%s\n' "$heads" | grep -c ' (head)$')"
          if [ "$count" -ne 1 ]; then
            echo "Expected exactly one Alembic head; found $count."
            exit 1
          fi
      - name: Verify MariaDB authentication concurrency contracts
        env:
          RENTIVO_TEST_MARIADB_URL: ${{ env.RENTIVO_DB_URL }}
        run: >-
          uv run --project backend --no-sync pytest
          backend/tests/repositories/test_mfa_repos.py
          -k mariadb -q
      - name: Upgrade an empty database to head
        run: uv run --project backend --no-sync alembic -c backend/alembic.ini upgrade head
      - name: Report the applied revision
        run: uv run --project backend --no-sync alembic -c backend/alembic.ini current

  compose-config:
    name: promoted Compose configuration
    runs-on: ubuntu-latest
    env:
      MYSQL_DATABASE: rentivo_compose_ci
      MYSQL_PASSWORD: ci-only-database-password
      MYSQL_ROOT_PASSWORD: ci-only-root-password
      MYSQL_USER: rentivo_compose_ci
      RENTIVO_APP_ENV_FILE: ${{ runner.temp }}/rentivo-compose-app.env
      RENTIVO_PUBLIC_ORIGIN: https://rentivo.example.test
      RENTIVO_TRUSTED_TLS_TERMINATOR_CIDR: 127.0.0.1/32
      RENTIVO_WEBAUTHN_RP_ID: rentivo.example.test
    steps:
      - uses: actions/checkout@v7
      - name: Create disposable application environment
        run: |
          umask 077
          printf '%s\n' \
            'RENTIVO_DB_URL=mysql+pymysql://rentivo_compose_ci:ci-only-database-password@db:3306/rentivo_compose_ci' \
            'RENTIVO_SECRET_KEY=ci-only-compose-secret-key-replace-after-job' \
            'RENTIVO_EMAIL_BACKEND=ses' \
            'RENTIVO_SES_REGION=us-east-1' \
            'RENTIVO_SES_FROM_EMAIL=noreply@rentivo.example.test' \
            'RENTIVO_STORAGE_BACKEND=s3' \
            'RENTIVO_S3_BUCKET=rentivo-compose-ci' \
            'RENTIVO_S3_REGION=us-east-1' \
            'RENTIVO_ENCRYPTION_BACKEND=kms' \
            'RENTIVO_KMS_KEY_ID=alias/rentivo-compose-ci' \
            'RENTIVO_KMS_REGION=us-east-1' \
            'RENTIVO_LOG_JSON=true' \
            > "$RENTIVO_APP_ENV_FILE"
      - name: Validate production Compose
        run: docker compose -f docker-compose.yml config --quiet
      - name: Validate development Compose
        run: docker compose -f docker-compose.yml -f docker-compose.dev.yml config --quiet

  production-stack:
    name: MariaDB-backed promoted production stack
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      MYSQL_DATABASE: rentivo_gate
      MYSQL_USER: rentivo_gate
      RENTIVO_APP_ENV_FILE: ${{ runner.temp }}/rentivo-gate-app.env
      RENTIVO_PUBLIC_ORIGIN: http://127.0.0.1:8080
      RENTIVO_TRUSTED_TLS_TERMINATOR_CIDR: 127.0.0.1/32
      RENTIVO_WEBAUTHN_RP_ID: 127.0.0.1
      STACK_OVERRIDE: ${{ runner.temp }}/rentivo-gate.override.yml
      STACK_PROJECT: rentivo-production-gate
    steps:
      - uses: actions/checkout@v7
      - name: Install Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci
      - name: Install Chromium
        working-directory: frontend
        run: npx playwright install --with-deps chromium
      - name: Create secure disposable stack environment
        shell: bash
        run: |
          set -euo pipefail
          umask 077
          db_password="$(openssl rand -hex 32)"
          db_root_password="$(openssl rand -hex 32)"
          app_secret="$(openssl rand -hex 48)"
          echo "::add-mask::$db_password"
          echo "::add-mask::$db_root_password"
          echo "::add-mask::$app_secret"
          {
            echo "MYSQL_PASSWORD=$db_password"
            echo "MYSQL_ROOT_PASSWORD=$db_root_password"
          } >> "$GITHUB_ENV"
          printf '%s\n' \
            "RENTIVO_DB_URL=mysql+pymysql://rentivo_gate:${db_password}@db:3306/rentivo_gate" \
            "RENTIVO_SECRET_KEY=${app_secret}" \
            'RENTIVO_EMAIL_BACKEND=local' \
            'RENTIVO_EMAIL_LOCAL_PATH=/app/outbox' \
            'RENTIVO_STORAGE_BACKEND=local' \
            'RENTIVO_STORAGE_LOCAL_PATH=/app/invoices' \
            'RENTIVO_ENCRYPTION_BACKEND=base64' \
            'RENTIVO_LOG_JSON=true' \
            > "$RENTIVO_APP_ENV_FILE"
          cat > "$STACK_OVERRIDE" <<'YAML'
          services:
            migrate:
              environment:
                RENTIVO_ENVIRONMENT: dev
            api:
              environment:
                RENTIVO_ENVIRONMENT: dev
                RENTIVO_PUBLIC_URL: http://127.0.0.1:8080
                RENTIVO_PUBLIC_APP_URL: http://127.0.0.1:8080
                RENTIVO_WEBAUTHN_ORIGIN: http://127.0.0.1:8080
                RENTIVO_WEBAUTHN_RP_ID: 127.0.0.1
                RENTIVO_COOKIE_SECURE: "false"
                RENTIVO_ACCESS_COOKIE_NAME: rentivo_access
                RENTIVO_CHALLENGE_COOKIE_NAME: rentivo_challenge
                RENTIVO_CSRF_COOKIE_NAME: rentivo_csrf
            worker:
              environment:
                RENTIVO_ENVIRONMENT: dev
          YAML
      - name: Start promoted production stack
        shell: bash
        run: |
          set -euo pipefail
          docker compose -p "$STACK_PROJECT" -f docker-compose.yml -f "$STACK_OVERRIDE" up --build -d
          migration_id="$(docker compose -p "$STACK_PROJECT" -f docker-compose.yml -f "$STACK_OVERRIDE" ps -aq migrate)"
          for attempt in $(seq 1 60); do
            state="$(docker inspect --format '{{.State.Status}}:{{.State.ExitCode}}' "$migration_id")"
            if [ "$state" = "exited:0" ]; then
              break
            fi
            if [[ "$state" == exited:* ]]; then
              echo "Migration failed with $state."
              exit 1
            fi
            [ "$attempt" -lt 60 ] || { echo "Migration did not finish."; exit 1; }
            sleep 2
          done
          for attempt in $(seq 1 90); do
            if curl --fail --silent --show-error http://127.0.0.1:8080/api/v1/ready >/dev/null; then
              exit 0
            fi
            [ "$attempt" -lt 90 ] || { echo "Production stack never became ready."; exit 1; }
            sleep 2
          done
      - name: Run production stack smoke
        run: ./scripts/smoke-production-stack.sh http://127.0.0.1:8080
      - name: Run real-stack Playwright project
        working-directory: frontend
        env:
          PLAYWRIGHT_PRODUCTION_STACK: "1"
          PLAYWRIGHT_BASE_URL: http://127.0.0.1:8080
        run: npm run e2e -- --project=production-stack
      - name: Capture production stack logs
        if: always()
        run: |
          mkdir -p artifacts
          docker compose -p "$STACK_PROJECT" -f docker-compose.yml -f "$STACK_OVERRIDE" ps -a > artifacts/production-stack-ps.txt
          docker compose -p "$STACK_PROJECT" -f docker-compose.yml -f "$STACK_OVERRIDE" logs --no-color > artifacts/production-stack.log
      - name: Upload production stack evidence
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: production-stack-results
          path: |
            artifacts/production-stack-ps.txt
            artifacts/production-stack.log
            frontend/test-results
          if-no-files-found: warn
          retention-days: 14
      - name: Stop production stack
        if: always()
        run: docker compose -p "$STACK_PROJECT" -f docker-compose.yml -f "$STACK_OVERRIDE" down --volumes --remove-orphans

  release-gate:
    name: complete-release-gate
    runs-on: ubuntu-latest
    needs: [backend, frontend, e2e, migrations, compose-config, production-stack]
    if: always()
    steps:
      - name: Verify every release gate succeeded
        env:
          NEEDS_JSON: ${{ toJSON(needs) }}
        run: |
          failures="$(printf '%s' "$NEEDS_JSON" | jq -r 'to_entries[] | select(.value.result != "success") | "\(.key)=\(.value.result)"')"
          if [ -n "$failures" ]; then
            printf 'Required release gates failed:\n%s\n' "$failures"
            exit 1
          fi

  production-images:
    name: immutable image build (${{ matrix.name }})
    runs-on: ubuntu-latest
    needs: release-gate
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: api
            dockerfile: backend/Dockerfile.api
            image: ghcr.io/${{ github.repository }}/api
            scope: production-api
          - name: worker
            dockerfile: backend/Dockerfile.worker
            image: ghcr.io/${{ github.repository }}/worker
            scope: production-worker
          - name: frontend
            dockerfile: frontend/Dockerfile
            image: ghcr.io/${{ github.repository }}/frontend
            scope: production-frontend
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/docker-build
        with:
          dockerfile: ${{ matrix.dockerfile }}
          image-name: ${{ matrix.image }}
          image-tag: ${{ github.sha }}
          cache-scope: ${{ matrix.scope }}
          push: "false"

  all-checks-pass:
    name: all-checks-pass
    runs-on: ubuntu-latest
    needs: [release-gate, production-images]
    if: always()
    steps:
      - name: Verify release gate and immutable builds succeeded
        env:
          GATE_RESULT: ${{ needs.release-gate.result }}
          IMAGES_RESULT: ${{ needs.production-images.result }}
        run: |
          if [ "$GATE_RESULT" != "success" ] || [ "$IMAGES_RESULT" != "success" ]; then
            echo "release-gate=$GATE_RESULT"
            echo "production-images=$IMAGES_RESULT"
            exit 1
          fi
