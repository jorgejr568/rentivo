RENTIVO — TRANSFERÊNCIA DE COBRANÇA
=====================================

{% if recipient_role == "previous_owner" -%}
A cobrança "{{ billing_name }}" foi transferida para outro proprietário.
{%- elif recipient_role == "destination_admin" -%}
A cobrança "{{ billing_name }}" foi transferida para sua organização.
{%- else -%}
A cobrança "{{ billing_name }}" mudou de proprietário.
{%- endif %}

Quem fez a transferência: {{ actor_email }}

{% include "_email_footer.txt" %}
