# Database (MariaDB)
RENTIVO_DB_URL=mysql+pymysql://rentivo:rentivo@localhost:3306/rentivo

# MariaDB container settings
MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=rentivo
MYSQL_USER=rentivo
MYSQL_PASSWORD=rentivo
MYSQL_PORT=3306

# Storage
RENTIVO_STORAGE_BACKEND=local
RENTIVO_STORAGE_LOCAL_PATH=./invoices

# S3 storage (set RENTIVO_STORAGE_BACKEND=s3 to enable)
RENTIVO_S3_BUCKET=
RENTIVO_S3_REGION=
RENTIVO_S3_ACCESS_KEY_ID=
RENTIVO_S3_SECRET_ACCESS_KEY=
RENTIVO_S3_ENDPOINT_URL=
RENTIVO_S3_PRESIGNED_EXPIRY=604800

# Web session secret (required for stable sessions across restarts)
LANDLORD_SECRET_KEY=change-me-in-production

# WebAuthn / Passkeys
LANDLORD_WEBAUTHN_RP_ID=localhost
LANDLORD_WEBAUTHN_RP_NAME=Landlord
LANDLORD_WEBAUTHN_ORIGIN=http://localhost:8000

# Logging
RENTIVO_LOG_LEVEL=INFO
RENTIVO_LOG_JSON=false

# Email
RENTIVO_EMAIL_BACKEND=local
RENTIVO_EMAIL_LOCAL_PATH=./outbox

# AWS SES (set RENTIVO_EMAIL_BACKEND=ses to enable)
RENTIVO_SES_REGION=
RENTIVO_SES_ACCESS_KEY_ID=
RENTIVO_SES_SECRET_ACCESS_KEY=
RENTIVO_SES_ENDPOINT_URL=
RENTIVO_SES_FROM_EMAIL=
RENTIVO_SES_CONFIGURATION_SET=

# Canonical app URL used in transactional emails
RENTIVO_PUBLIC_APP_URL=http://localhost:8000

# Cloudflare Turnstile bot protection (set both to enable, leave both empty to disable)
RENTIVO_TURNSTILE_SITE_KEY=
RENTIVO_TURNSTILE_SECRET_KEY=
RENTIVO_TURNSTILE_VERIFY_URL=https://challenges.cloudflare.com/turnstile/v0/siteverify

# Field encryption (PIX keys, merchant info, TOTP secrets)
# Use RENTIVO_ENCRYPTION_BACKEND=kms in production.
# The default (base64) is reversible obfuscation, NOT encryption — it exists so
# every environment has the same "stored value differs from plaintext" contract;
# anyone with database access can trivially decode it.
# When switching to kms, run the backfill:
#   .venv/bin/python -m rentivo.scripts.backfill_encryption
RENTIVO_ENCRYPTION_BACKEND=base64

# AWS KMS (only used when RENTIVO_ENCRYPTION_BACKEND=kms)
# IMPORTANT: enable key deletion protection on this KMS key — losing the key
# means losing all encrypted PII permanently.
RENTIVO_KMS_KEY_ID=
RENTIVO_KMS_REGION=
RENTIVO_KMS_ACCESS_KEY_ID=
RENTIVO_KMS_SECRET_ACCESS_KEY=
RENTIVO_KMS_ENDPOINT_URL=

# Dashboard cache (none/memory/redis)
RENTIVO_DASHBOARD_CACHE_BACKEND=none
RENTIVO_DASHBOARD_CACHE_TTL_SECONDS=30
RENTIVO_DASHBOARD_CACHE_MAX_ENTRIES=1000
